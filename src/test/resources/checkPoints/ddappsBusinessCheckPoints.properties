checkpoint.1.name=From INITIATING(1) to IN_IVR(2)
checkpoint.1.pattern=INFO IVRImpl  - Status of callback request id %s updated from INITIATING(1) to IN_IVR(2).
checkpoint.1.token=637

checkpoint.2.name=From IN_IVR(2) to COMPLETING(3)
checkpoint.2.pattern=INFO IVRImpl  - Status of callback request id %s updated from IN_IVR(2) to COMPLETING(3).
checkpoint.2.token=637

checkpoint.3.name=From COMPLETING(3) to DELIVERED(4)
checkpoint.3.pattern=INFO GeneralImpl  - Status of callback request id %s updated from COMPLETING(3) to DELIVERED(4), and added a new callTrace with status 0.
checkpoint.3.token=637


